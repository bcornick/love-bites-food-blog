import { Architects_Daughter, Quicksand } from 'next/font/google';

export const quicksand = Quicksand({
  subsets: ['latin'],
});

export const architects_daughter = Architects_Daughter({
  subsets: ['latin'],
  weight: ['400'],
});
